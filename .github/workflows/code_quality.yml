name: Python Code Quality

on:
    push:
    pull_request:
    schedule:
        - cron: '0 6 * * 1' # Weekly on Monday at 06:00 UTC

permissions: read-all

env:
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
    lock_file:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
            - uses: ./.github/actions/setup
            - name: Install just
              uses: taiki-e/install-action@9e1e5806d4a4822de933115878265be9aaa786d9 # v2.82.2
              with:
                  tool: just
            - name: Check uv.lock is up to date
              if: github.actor != 'dependabot[bot]'
              run: just check-lock

    linting:
        runs-on: ubuntu-latest
        needs: [lock_file]
        steps:
            - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
            - uses: ./.github/actions/setup
            - name: Install just
              uses: taiki-e/install-action@9e1e5806d4a4822de933115878265be9aaa786d9 # v2.82.2
              with:
                  tool: just
            - name: Lint with ruff
              run: just check-lint-only

    formatting:
        runs-on: ubuntu-latest
        needs: [lock_file]
        steps:
            - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
            - uses: ./.github/actions/setup
            - name: Install just
              uses: taiki-e/install-action@9e1e5806d4a4822de933115878265be9aaa786d9 # v2.82.2
              with:
                  tool: just
            - name: Check formatting
              run: just check-format-only

    type_consistency:
        runs-on: ubuntu-latest
        needs: [lock_file]
        steps:
            - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
            - uses: ./.github/actions/setup
            - name: Install just
              uses: taiki-e/install-action@9e1e5806d4a4822de933115878265be9aaa786d9 # v2.82.2
              with:
                  tool: just
            - name: Type check with ty
              run: just check-types

    # type_budget:
    #     runs-on: ubuntu-latest
    #     needs: [lock_file]
    #     steps:
    #         - uses: actions/checkout@v6
    #         - uses: ./.github/actions/setup
    #         - name: Install just
    #           run: |
    #               curl -fsSL "https://github.com/casey/just/releases/download/${JUST_VERSION}/just-${JUST_VERSION}-x86_64-unknown-linux-musl.tar.gz" | tar -xz
    #               sudo install -m 755 just /usr/local/bin/just
    #               just --version
    #         - run: uv sync --locked --dev
    #         - name: Enforce ty diagnostics budget
    #           env:
    #               TY_MAX_DIAGNOSTICS: '0'
    #           run: just check-types-budget
